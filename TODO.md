# TODO

- [ ] Inspect Experimentno12 (Assignment 12) and Experiment 13 (Assignment 13) styling for unreadable font (black text on dark background).
- [ ] Update CSS so transformed/embedded content uses white (or readable) text color.
- [ ] Ensure both pages render correctly after transformation output is inserted.
- [ ] (If needed) Update XSL embedded styles so tables/text inside the transformed HTML are readable.
- [ ] Verify by opening both pages in browser.

